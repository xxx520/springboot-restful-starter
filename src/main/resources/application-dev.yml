# 开发环境配置文件

server:
  port: 8081     # Springboot占用端口

spring:
  datasource:
    druid:
      driver-class-name: com.mysql.jdbc.Driver     # 数据库驱动
      url: jdbc:mysql://127.0.0.1:3306/seed?serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true&useSSL=false     # 数据库链接
      username: test
      password: 123456
# 数据库密码
  devtools:
    restart:
      enabled: true     # 开启热更新